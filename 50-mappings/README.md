# 50-mappings

The web. Where the pillars connect: CSDM classes ↔ CCH instances ↔ Dynatrace entities, plus architectural decisions explaining *why* mappings are modelled the way they are.

This is where the vault stops being a filing cabinet and starts being a thinking instrument.
